import { useRef, useState } from 'react'
import { ArrowLeft, ArrowRight, ArrowUpRight, AudioLines, Bot, Check, ChevronRight, CircleCheck, Code2, FileText, Folder, LayoutGrid, ScanLine, Search, Settings2, Workflow } from 'lucide-react'
import { Button, buttonVariants } from '@/components/ui/button'
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from '@/components/ui/dialog'
import { Reveal } from './motion'
import { cn } from '@/lib/utils'

const projects = [
  { title: 'AI Meeting Intelligence', category: 'PRODUCTIVITY · GENERATIVE AI', stack: ['Python', 'LLMs', 'React'], description: 'Every conversation. A clear next step.', challenge: 'Important decisions and follow-ups get lost between meetings, notes, and disconnected tools.', approach: 'A consent-aware meeting workspace that brings transcripts, concise summaries, and assigned action items together in one place.', features: ['Searchable, speaker-aware transcripts', 'Source-linked summaries and decisions', 'Clear action items with owners'], type: 'meeting' },
  { title: 'AI Document Processing', category: 'OPERATIONS · INTELLIGENT AUTOMATION', stack: ['Python', 'OCR', 'FastAPI'], description: 'From unstructured documents to useful data.', challenge: 'Manual document entry slows operations and creates opportunities for errors.', approach: 'A document pipeline that extracts structured fields, validates the output, and sends uncertain results to a human reviewer.', features: ['Document classification and extraction', 'Confidence-based review workflow', 'Validated export to business systems'], type: 'document' },
  { title: 'Business Automation Platform', category: 'ENTERPRISE · WORKFLOW AUTOMATION', stack: ['Python', 'APIs', 'PostgreSQL'], description: 'Connect your tools. Move your business.', challenge: 'Repetitive handoffs between systems leave teams spending too much time moving information.', approach: 'A connected workflow layer with clear triggers, approvals, and a visible audit trail for each business process.', features: ['Event-driven business workflows', 'Human approval checkpoints', 'Centralized execution history'], type: 'automation' },
  { title: 'Custom SaaS Platform', category: 'SOFTWARE · PRODUCT ENGINEERING', stack: ['Next.js', 'TypeScript', 'PostgreSQL'], description: 'A platform built around the way you work.', challenge: 'Off-the-shelf tools do not always fit unique operating models and user needs.', approach: 'A tailored product foundation with intuitive workspaces, role-based access, and a modular architecture that can evolve.', features: ['Purpose-built user experiences', 'Secure multi-role workspaces', 'Maintainable, modular architecture'], type: 'saas' },
  { title: 'Computer Vision Solution', category: 'VISION · APPLIED AI', stack: ['Python', 'Vision Models', 'FastAPI'], description: 'See the details. Surface the possibilities.', challenge: 'Visual inspection can be repetitive, inconsistent, and difficult to scale.', approach: 'An assisted visual review pipeline that highlights candidate objects or anomalies for a person to validate.', features: ['Task-specific visual detection', 'Human review of model predictions', 'Integration with existing image pipelines'], type: 'vision' },
]

function ProjectVisual({ type }: { type: string }) {
  return <div className={cn('project-visual', `visual-${type}`)} aria-hidden="true"><div className="mini-app"><div className="mini-sidebar"><span className="mini-app-logo"><AudioLines size={15} /></span><LayoutGrid size={12} /><Folder size={12} /><Settings2 size={12} /></div><div className="mini-main"><div className="mini-top"><strong>{type === 'meeting' ? 'Meetings' : type === 'document' ? 'Document workspace' : type === 'automation' ? 'Workflow builder' : type === 'saas' ? 'Your workspace' : 'Vision workspace'}</strong><Search size={11} /><span className="mini-avatar">S</span></div><div className="mini-body">{type === 'meeting' ? <><div className="mini-subheading"><span><span className="mini-eyebrow">TEAM WORKSPACE</span><strong>Great conversations.<br />Even better clarity.</strong></span><span className="mini-add">+ New meeting</span></div><div className="meeting-wave"><AudioLines size={18} /><span className="wave-bars">{Array.from({ length: 40 }, (_, i) => <i key={i} style={{ height: `${9 + ((i * 17) % 27)}px` }} />)}</span><span>12:48</span></div><div className="mini-columns"><div className="mini-panel"><strong>Meeting summary</strong><span className="skeleton-line" /><span className="skeleton-line short" /><span className="skeleton-line" /><span className="skeleton-line short" /></div><div className="mini-panel"><strong>Action items</strong><span className="mini-action"><CircleCheck size={9} />Review project roadmap</span><span className="mini-action"><CircleCheck size={9} />Share design direction</span><span className="mini-action"><CircleCheck size={9} />Schedule next steps</span></div></div></> : type === 'document' ? <><div className="mini-subheading"><span><span className="mini-eyebrow">SMART EXTRACTION</span><strong>Data, without the busywork.</strong></span></div><div className="doc-workspace"><div className="document-sheet"><FileText size={21} /><strong>Invoice</strong><span className="skeleton-line" /><span className="skeleton-line short" /><div className="document-focus"><span /><span /><span /></div><span className="skeleton-line" /><span className="skeleton-line short" /></div><ChevronRight size={17} /><div className="extracted-fields"><span><Bot size={13} />Extracted fields</span>{['Document type', 'Invoice number', 'Date', 'Amount'].map(x => <div key={x}><span>{x}</span><Check size={10} /></div>)}<span className="mini-success">Ready for review</span></div></div></> : type === 'automation' ? <><div className="mini-subheading"><span><span className="mini-eyebrow">CONNECTED OPERATIONS</span><strong>Let the work flow.</strong></span><span className="mini-success">Active</span></div><div className="flow-canvas"><div className="flow-box"><FileText size={16} /><span>New request</span><Check size={11} /></div><div className="flow-line" /><div className="flow-box"><Bot size={16} /><span>AI processing</span><Check size={11} /></div><div className="flow-split"><div className="flow-box"><CircleCheck size={14} /><span>Approval</span></div><div className="flow-box"><Workflow size={14} /><span>Sync data</span></div></div></div></> : type === 'saas' ? <><div className="mini-subheading"><span><span className="mini-eyebrow">A SINGLE SOURCE OF TRUTH</span><strong>Everything in its place.</strong></span></div><div className="workspace-tiles">{['Projects', 'Tasks', 'Team'].map(x => <div key={x}><LayoutGrid size={17} /><strong>{x}</strong></div>)}</div>{['Product roadmap', 'Design system', 'Sprint planning'].map(x => <div className="mini-row" key={x}><Folder size={12} />{x}<span>In progress</span></div>)}</> : <><div className="mini-subheading"><span><span className="mini-eyebrow">COMPUTER VISION</span><strong>A new way to see.</strong></span><ScanLine size={21} /></div><div className="vision-scene"><div className="vision-object object-a" /><div className="vision-object object-b" /><div className="vision-target target-a"><span>Object detected</span></div><div className="vision-target target-b"><span>Review candidate</span></div><div className="scan-line" /></div></>}</div></div></div><span className="visual-badge"><span />CONCEPT PREVIEW</span></div>
}

export function Projects() {
  const slider = useRef<HTMLDivElement>(null)
  const [active, setActive] = useState<number | null>(null)
  const [atStart, setAtStart] = useState(true)
  const [atEnd, setAtEnd] = useState(false)
  function scroll(direction: number) { const node = slider.current; if (node) node.scrollBy({ left: direction * (node.clientWidth * 0.7), behavior: window.matchMedia('(prefers-reduced-motion: reduce)').matches ? 'instant' : 'smooth' }) }
  return <section id="projects" className="projects-section section-space"><div className="container"><Reveal className="section-heading"><div><span className="eyebrow"><span />POSSIBILITIES IN PRACTICE</span><h2>From ideas <span className="muted-heading">to impact.</span></h2></div><div className="project-heading-right"><p>A look at what we can build together.<br />Explore our solution concepts.</p><div className="slider-controls"><Button variant="outline" size="icon-lg" aria-label="Previous projects" disabled={atStart} onClick={() => scroll(-1)}><ArrowLeft /></Button><Button variant="outline" size="icon-lg" aria-label="Next projects" disabled={atEnd} onClick={() => scroll(1)}><ArrowRight /></Button></div></div></Reveal><div className="projects-slider" ref={slider} onScroll={event => { const el = event.currentTarget; setAtStart(el.scrollLeft < 5); setAtEnd(el.scrollLeft + el.clientWidth >= el.scrollWidth - 5) }}>{projects.map((project, i) => <article className="project-card" key={project.title}><button className="project-image-button" onClick={() => setActive(i)} aria-label={`View ${project.title} concept`}><ProjectVisual type={project.type} /></button><div className="project-meta"><span>{project.category}</span><span>0{i + 1}</span></div><h3><button onClick={() => setActive(i)}>{project.title}<ArrowUpRight size={21} /></button></h3><p>{project.description}</p><div className="project-card-footer"><div className="project-stack">{project.stack.map(item => <span key={item}>{item}</span>)}</div><button className="project-detail-link" onClick={() => setActive(i)} aria-label={`Read ${project.title} concept`}>Explore <ArrowRight size={13} /></button></div></article>)}</div><div className="projects-bottom"><span><Code2 size={14} />Illustrative concepts. Real engineering possibilities.</span><span>BUILT WITH PURPOSE. DESIGNED FOR IMPACT.</span></div></div><Dialog open={active !== null} onOpenChange={open => { if (!open) setActive(null) }}><DialogContent className="project-dialog sm:max-w-2xl">{active !== null && <><DialogHeader><span className="eyebrow">SOLUTION CONCEPT · NOT A CLIENT CASE STUDY</span><DialogTitle>{projects[active].title}</DialogTitle><DialogDescription>{projects[active].description}</DialogDescription></DialogHeader><ProjectVisual type={projects[active].type} /><div className="dialog-project-details"><div><h4>The challenge</h4><p>{projects[active].challenge}</p></div><div><h4>Our approach</h4><p>{projects[active].approach}</p></div></div><ul className="capability-list">{projects[active].features.map(feature => <li key={feature}><Check size={15} />{feature}</li>)}</ul><a href="#contact" className={buttonVariants({ variant: 'brand', size: 'hero' })} onClick={() => setActive(null)}>Build something like this <ArrowUpRight data-icon="inline-end" /></a></>}</DialogContent></Dialog></section>
}
